## Contacts & Phone

### hooks/useContacts.js
Hook: `useContacts()`
Returns: `{ contacts, favorites, callState: 'idle'|'ringing'|'connected'|'ended', activeContact, callDuration, callDurationFormatted, call(contact), hangUp() }`
Simulated phone with 8 German contacts. Ringing → connected after 2s.
